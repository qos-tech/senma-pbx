---
name: qos-run
description: Run the shared Ralph orchestrator with Codex or Claude.
---

Resolve the repository root and execute `./scripts/ralph.sh` with the user's arguments. Default to `--engine codex` when no engine is supplied. Verify a clean Git working tree and report Ralph's actual result without bypassing gates.
