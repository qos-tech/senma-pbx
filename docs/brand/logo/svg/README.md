# SVG master

The approved V2 artwork is currently raster concept art. A fake auto-traced SVG is intentionally not included. Redraw the approved geometry as native vector paths before declaring an SVG the canonical production logo.
